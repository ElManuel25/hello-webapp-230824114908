name: Docker Image

on:
    push
jobs:
    build_and_publish:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Build and push the Image
              run: |
                docker login --username elmanuel25 --password ${{ secrets.GH_PAT }} gdlcemmanuelsamuel.azurecr.io
                docker build . gdlcemmanuelsamuel.azurecr.io/petclinic-app:latest
                docker push gdlcemmanuelsamuel.azurecr.io/petclinic-app:latest
